package com.redis.om.hybrid;

// Demo-level context test is intentionally omitted because it requires
// a running Redis Stack instance. Integration tests for hybrid search
// are in the tests module (EntityStreamHybridQueryTests).
